# 作業 08 — Rust 入門

第一個 Rust 程式。

## 說明

`hello.rs` — 基本 Rust 程式，在終端機輸出 "Hello, world!"。

## 編譯與執行

```sh
rustc hello.rs
./hello
```

或使用 Cargo：

```sh
cargo new --bin hello08
# 將 hello.rs 內容放入 src/main.rs
cargo run
```
